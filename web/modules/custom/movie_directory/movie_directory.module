<?php

/**
 * Implements hook_theme().
 */
function movie_directory_theme($existing, $type, $theme, $path) {
  return [
    'movie_listing' => [
      'variables' => ['content' => NULL],
    ],
  ];
}

